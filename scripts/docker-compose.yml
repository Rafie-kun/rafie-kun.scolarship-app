version: "3.8"

services:
  scholarpath:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scholarpath_ecosystem
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: always
